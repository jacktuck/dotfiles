# dotfiles
My personal dotfiles; buyer beware.

## `install`
Installs dependencies via Homebrew and bootstraps the dotfiles with `stow`.

## `install-apps`
Installs a few Mac App Store apps via `mas`.
